syntax = "proto3";

package system;

import "common.proto";

option go_package = "github.com/DOGTT/dm-api-server/api/base;api";

// 系统通知
message NotifyInfo {
  // 通知id
  string id = 1;
  // 正文内容
  string content = 5;

  int64 created_at = 31;
  int64 updated_at = 32;
}
